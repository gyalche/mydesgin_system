# @hamro-design-system/ui

Production-grade React components built on the shared token contract.

Package rules:

- consume semantic tokens, not raw palette values
- prefer composition over large prop APIs
- use Radix primitives for complex accessibility-heavy controls
- keep public APIs consistent across variant, size, tone, and state
- keep business workflows out of the package
