# @hamro-design-system/icons

Owns the icon pipeline and typed icon exports.

Recommended rules:

- source SVGs remain unstyled
- icons inherit current color by default
- size is controlled through component props or CSS
- generated assets should not embed product-only business logic
