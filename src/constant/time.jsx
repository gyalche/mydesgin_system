export const TIME_COLUMNS = Object.freeze(['hour', 'minute', 'ampm']);

export const AM_PM_VALUES = Object.freeze([
  { name: 'AM', value: 'am' },
  { name: 'PM', value: 'pm' },
]);

export const START_TIME = Object.freeze({
  hour: 'hour',
  minute: 'minute',
  ampm: 'ampm',
});

export const END_TIME = Object.freeze({
  hour: 'hourEnd',
  minute: 'minuteEnd',
  ampm: 'ampmEnd',
});
